import React, { useState, useRef } from "react";
import "../Styles/notes.css";

export const Notes = (paragraphs) => {

  return (
    <div className="notes-container">
      <div className="notes-header-container">
        <h1 className="notes-title">Notes</h1>
      </div>
    </div>
  );
};
